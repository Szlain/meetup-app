import { render, screen } from '@testing-library/react';

describe('As a user when I visit the homepage', () => {
  describe('I should see a list of meetups', () => {
    test.todo('They should contain an image')
  });
});